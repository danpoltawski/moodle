build: false
shallow_clone: true
platform: x86
clone_folder: c:\projects\moodle

services:
  - postgresql

cache:
    - c:\php #-> appveyor.yml
    - '%LOCALAPPDATA%\Composer'
    - vendor # composer dirs..
    - node_modules

init:
    - SET PATH=C:\Program Files\PostgreSQL\9.3\bin\;c:\php;%PATH%
    - SET COMPOSER_NO_INTERACTION=1
    - SET PHP=1
    - SET ANSICON=121x90 (121x90)
    - SET PGUSER=postgres
    - SET PGPASSWORD=Password12!


install:
    - IF EXIST c:\php (SET PHP=0) ELSE (mkdir c:\php)
    - cd c:\php
    - IF %PHP%==1 appveyor DownloadFile http://windows.php.net/downloads/releases/php-5.6.18-nts-Win32-VC11-x86.zip
    - IF %PHP%==1 7z x php-5.6.18-nts-Win32-VC11-x86.zip -y >nul
    - IF %PHP%==1 del /Q *.zip
    - IF %PHP%==1 copy /Y php.ini-development php.ini
    - IF %PHP%==1 echo max_execution_time=1200 >> php.ini
    - IF %PHP%==1 echo date.timezone="UTC" >> php.ini
    - IF %PHP%==1 echo extension_dir=ext >> php.ini
    - IF %PHP%==1 echo extension=php_openssl.dll >> php.ini
    - IF %PHP%==1 echo extension=php_intl.dll >> php.ini
    - IF %PHP%==1 echo extension=php_mbstring.dll >> php.ini
    - IF %PHP%==1 echo extension=php_fileinfo.dll >> php.ini
    - IF %PHP%==1 echo extension=php_curl.dll >> php.ini
    - IF %PHP%==1 echo extension=php_xmlrpc.dll >> php.ini
    - IF %PHP%==1 echo @php %%~dp0composer.phar %%* > composer.bat
    - cd c:\projects\moodle
    - appveyor DownloadFile https://getcomposer.org/composer.phar
    #- composer install --prefer-dist --no-interaction --ansi --no-progress

before_test:
    # Services are started after install..
    - createdb moodle
    - cd c:\projects\moodle
    - php admin\cli\install.php --wwwroot="http://127.0.0.1" --dbtype=pgsql --dbhost="127.0.0.1" --dbuser=postgres --dbpass="Password12!" --dbname=moodle --fullname=CI --shortname="ci" --adminpass=test --agree-license --allow-unstable --non-interactive
    - php admin\tool\phpunit\cli\init.php

test_script:
    - cd c:\projects\moodle
    - vendor\bin\phpunit
